/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // images: {
  //   unoptimized: true,
  //   path: './',
  // },
  // assetPrefix: './',
}

module.exports = nextConfig
